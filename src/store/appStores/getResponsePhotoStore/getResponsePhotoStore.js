const INITIAL_STATE = {
  responsePhoto: [],
};

const responsePhotoStoreReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default responsePhotoStoreReducer;
