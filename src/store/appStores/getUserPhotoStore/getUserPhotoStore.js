const INITIAL_STATE = {
  userPhoto: [],
};

const getUserPhotoStoreReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default getUserPhotoStoreReducer;
