const INITIAL_STATE = {
  defaultPhoto: [],
};

const defaultPhotoStoreReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default defaultPhotoStoreReducer;
